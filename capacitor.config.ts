import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId:'org.babulilm.app',
  appName:'Bab UL Ilm',
  webDir:'dist',
  server:{androidScheme:'https'},
}

export default config
